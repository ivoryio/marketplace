{
  "name": "catalog",
  "version": "0.1.0",
  "description": "POD_NAME microservice",
  "main": "app.js",
  "scripts": {
    "build": "yarn && mkdir -p build && aws cloudformation package --template-file infrastructure.yaml --output-template-file build/infrastructure.packaged.yaml",
    "deploy": "aws cloudformation deploy --template-file build/infrastructure.packaged.yaml --capabilities CAPABILITY_IAM",
    "test": "mocha",
    "test:unit": "CS_DOMAIN_NAME=catalog-search REGION=us-east-1 mocha test/unit",
    "test:integration": "SEARCH_HOSTNAME_SECRET=CloudSearch/Hostname REGION=us-east-1 TABLE_NAME=catalog_dev mocha test/integration"
  },
  "author": "Ivory",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^15.0.1",
    "aws-serverless-express": "^3.3.5",
    "body-parser": "^1.18.3",
    "chai": "^4.2.0",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "moment": "^2.24.0",
    "shortid": "^2.2.14",
    "uuid": "^3.3.2"
  }
}
